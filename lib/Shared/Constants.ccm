module;
#include <array>
export module pr.constants;
import pr.utils;
import pr.cards;

export namespace pr::constants {
constexpr usz StartingWordSize = 6;
constexpr usz PlayersPerGame = 2;
constexpr usz MaxSoundStackSize = 2;
using Word = std::array<CardId, StartingWordSize>;
}
